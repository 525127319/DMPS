import React, { Component } from "react";
import Tool from '@/components/Tool/Tool';
export default class SixHour extends Component {
    render() {
        return(
            <Tool type={2}/>
        );
    };
}